# Normally this file shouldn't be checked into git, but this project is an exception because 
# I'm documenting what I've done in this tutorial
import os 

SECRET_KEY = 'ThisIsATutorialProject' # For generating real ones, see secrets.token_hex()
